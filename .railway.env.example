# Copy this to .railway.env and configure for Railway deployment

# Google OAuth2 Configuration
GOOGLE_CLIENT_ID=your-google-client-id
GOOGLE_CLIENT_SECRET=your-google-client-secret

# JWT Secret (generate a secure random string)
JWT_SECRET=your-secure-jwt-secret-key

# LLM Provider API Key (optional, uses mock provider if not set)
LLM_API_KEY=your-llm-api-key

# Railway will automatically configure:
# - MYSQLURL (provided by Railway MySQL service)
# - PORT (provided by Railway)
# - RAILWAY_PUBLIC_DOMAIN (provided by Railway)
